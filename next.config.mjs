/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["images.unsplash.com","i.pravatar.cc","img.freepik.com","upload.wikimedia.org","upload.wikimedia.org"]
    }
};

export default nextConfig;
